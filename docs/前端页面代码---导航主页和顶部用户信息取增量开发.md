##    增量开发检查清单 v3.0

### 📋 基于文档更新的增量开发需求

**文档变更摘要：**
- ✅ 新增：主页作为系统导航中心
- ✅ 新增：所有页面（除认证页面外）必须包含顶部用户信息区域  
- ✅ 更新：登录/注册成功后默认跳转到主页
- ✅ 更新：其他操作成功后跳转到主页

---

#### 1. 主页功能新增
- [ ] **检查主页文件状态**
  - [ ] 确认 `/pages/index/index.vue` 是否存在
  - [ ] 如果不存在，创建主页文件
  - [ ] 如果存在，检查是否需要功能升级

- [ ] **实现系统导航中心功能**
  - [ ] 添加个性化欢迎区域（显示用户名和登录时间）
  - [ ] 实现三个功能模块卡片：
    - [ ] 用户管理模块卡片（图标、名称、描述、状态指示）
    - [ ] 视频下载模块卡片（图标、名称、描述、状态指示）
    - [ ] 直播模块卡片（图标、名称、描述、状态指示）
  - [ ] 添加系统状态栏（显示模块运行状态、用户权限等）

- [ ] **多终端适配实现**
  - [ ] H5端：网格布局（2列或3列），支持鼠标悬停
  - [ ] App端：垂直列表布局，支持触摸滑动
  - [ ] PC端：居中布局，最大宽度限制

#### 2. 顶部用户信息区域检查与添加
- [ ] **页面清单检查**
  - [ ] `/pages/index/index.vue` (主页)
  - [ ] `/pages/user/profile.vue` (个人资料页)
  - [ ] `/pages/user/edit-profile.vue` (编辑资料页)
  - [ ] `/pages/user/bind-phone.vue` (绑定手机号页)
  - [ ] `/pages/user/change-password.vue` (修改密码页)
  - [ ] `/pages/membership/products.vue` (会员产品列表页)
  - [ ] `/pages/membership/my-memberships.vue` (我的订阅页)

- [ ] **顶部用户信息区域功能实现**
  - [ ] 显示当前登录用户名（从JWT Token中解析）
  - [ ] 显示用户头像（如果JWT Token中包含相关信息）
  - [ ] 提供登出按钮（清理本地Token并跳转登录页）
  - [ ] 实现用户下拉菜单，包含：
    - [ ] "个人信息"选项：跳转地址 uni.navigateTo({url:`pages/user/profile`})
    - [ ] "主页"选项：跳转地址 uni.navigateTo({url:`pages/user/profile`})
  - [ ] 使用 `window.location.href` 进行页面跳转，确保完全跳转

#### 3. 跳转逻辑更新
- [ ] **登录成功跳转更新**
  - [ ] 用户名密码登录成功后跳转到主页 (`/pages/index/index`)
  - [ ] 第三方登录成功后跳转到主页
  - [ ] SSO登录成功后跳转到主页

- [ ] **注册成功跳转更新**
  - [ ] 普通注册成功后跳转到主页
  - [ ] 第三方注册成功后跳转到主页

- [ ] **其他操作跳转更新**
  - [ ] 编辑资料成功后跳转到主页
  - [ ] 绑定手机号成功后跳转到主页
  - [ ] 修改密码成功后跳转登录页（需要重新登录）

#### 4. 代码质量检查
- [ ] **遵循现有规范**
  - [ ] 使用 `constants/api.ts` 中的常量
  - [ ] 遵循状态命名约定（`formData`, `isLoading`）
  - [ ] 使用 `@import '@/styles/global.scss'` 和断点变量
  - [ ] 支持条件编译（H5/App）

- [ ] **安全规范检查**
  - [ ] 所有API请求通过 `BASE_API_URL` 常量拼接
  - [ ] 敏感信息使用加密存储
  - [ ] 用户权限验证
  - [ ] XSS防护

#### 5. 测试验证清单
- [ ] **功能测试**
  - [ ] 主页正常显示三个功能模块
  - [ ] 顶部用户信息区域在所有页面正常显示
  - [ ] 跳转逻辑正确执行
  - [ ] 多终端适配正常工作

- [ ] **兼容性测试**
  - [ ] H5端功能正常
  - [ ] App端功能正常
  - [ ] 响应式布局正常

---
